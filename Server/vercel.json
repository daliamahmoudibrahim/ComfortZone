{
  "version": 2,
  "rewrites": [{ "source": "/(.*)", "destination": "/" }],
  "routes": [{ "src": "/(.*)", "dest": "server.js" }],
  "builds": [{ 
    "src": "server.js",
   "use": "@vercel/node" }]
}